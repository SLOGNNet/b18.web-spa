let config = {
  "accessControlAllowOrigin": "*",
  'accessControlAllowMethods': 'GET, POST, OPTIONS, PUT, DELETE',
  'accessControlAllowHeaders': 'Content-Type, Access-Control-Allow-Headers, Authorization, X-Requested-With, enctype'
}
export default config;
